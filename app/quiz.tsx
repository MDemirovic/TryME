import PlaceholderFeatureScreen from "@/src/screens/PlaceholderFeatureScreen";

export default function QuizRoute() {
  return (
    <PlaceholderFeatureScreen
      title="Quiz placeholder"
      body="Quiz auth wiring is ready for the new flow. The real quiz experience can be reconnected next."
    />
  );
}
