import PlaceholderFeatureScreen from "@/src/screens/PlaceholderFeatureScreen";

export default function PaywallRoute() {
  return (
    <PlaceholderFeatureScreen
      title="Paywall placeholder"
      body="Subscription UX can plug back in after the Supabase-backed auth and document flow is stable."
    />
  );
}
