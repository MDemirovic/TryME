import PlaceholderFeatureScreen from "@/src/screens/PlaceholderFeatureScreen";

export default function CallRoute() {
  return (
    <PlaceholderFeatureScreen
      title="AI Call placeholder"
      body="The voice flow is intentionally out of the auth integration pass. This route is kept safe as a placeholder."
    />
  );
}
